---
"codex-relay": patch
"@codex-relay/mobile": patch
---

Default to GPT-6 Astra when available while preserving explicit model selections. Warn when the connected codex-relay package is older than 1.5.0. This mobile OTA requires the Expo 57 App Store 1.5.0 binary and targets app version 1.5.0 through the existing release workflow.
